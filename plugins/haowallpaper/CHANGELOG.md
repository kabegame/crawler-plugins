# 哲风壁纸更新日志

## 0.2.0

- 新增壁纸基本信息、发布者与相关标签的冻结详情展示。
- 新增结构化 metadata 迁移，兼容 `schema 1` 及更早版本的数据。
- 详情模板只使用爬取时保存的结构化字段，不保存页面 HTML，也不动态请求详情接口。
- 新增按目录、标签和色彩浏览的 PathQL Provider。
- 标签超过 100 个时按 `page-N` 分页，每页最多显示 100 个标签。
