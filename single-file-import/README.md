# 单文件导入插件（single-file-import）

## 这是做什么的？

用于从本地选择**单个图片文件**导入到图库（脚本只入队一次）。

## 用户文档

应用内展示的用户文档在：

- `doc_root/doc.md`

## 打包

在仓库根目录执行：

```bash
npm run package-plugin
```

打包后输出到 `test_plugin_packed/single-file-import.kgpg`。


