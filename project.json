{
  "name": "crawler-plugins",
  "namedInputs": {
    "pluginFiles": ["{projectRoot}/plugins/**/*"],
    "localImportFiles": ["{projectRoot}/plugins/local-import/**/*"],
    "packageScript": ["{projectRoot}/package-plugin.ts"],
    "packageConfig": [
      "{projectRoot}/package.tson",
      "{projectRoot}/pnpm-lock.yaml",
      "{projectRoot}/project.tson"
    ],
    "normalInputs": ["pluginFiles", "packageScript", "packageConfig"],
    "localInputs": ["localImportFiles", "packageScript", "packageConfig"]
  },
  "targets": {
    "package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun package-plugin.ts",
        "cwd": "{projectRoot}"
      },
      "cache": true,
      "inputs": ["normalInputs"],
      "outputs": ["{projectRoot}/packed/**/*"]
    },
    "package-local": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun package-plugin.ts --only local-import",
        "cwd": "{projectRoot}"
      },
      "cache": true,
      "inputs": ["localInputs"],
      "outputs": ["{projectRoot}/packed/**/*"]
    },
    "package-to-resources": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun package-plugin.ts --out-dir ../src-tauri/app-main/resources/plugins",
        "cwd": "{projectRoot}"
      },
      "cache": true,
      "inputs": ["normalInputs"],
      "outputs": ["{workspaceRoot}/src-tauri/app-main/resources/plugins/**/*"]
    },
    "package-local-to-resources": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun package-plugin.ts --only local-import --out-dir ../src-tauri/app-main/resources/plugins",
        "cwd": "{projectRoot}"
      },
      "cache": true,
      "inputs": ["localInputs"],
      "outputs": ["{workspaceRoot}/src-tauri/app-main/resources/plugins/**/*"]
    }
  }
}
